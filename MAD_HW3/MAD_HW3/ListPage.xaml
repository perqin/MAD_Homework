<Page
    x:Class="MAD_HW3.ListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MAD_HW3"
    xmlns:vm="using:MAD_HW3.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Page.Resources>
        <vm:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Page.Resources>

    <ListView
        x:Name="TodoListView"
        ItemsSource="{x:Bind TodoVM.Todos}"
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" ItemClick="TodoListView_ItemClick" SelectionChanged="TodoListView_SelectionChanged" IsItemClickEnabled="True">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="vm:Todo">
                <UserControl>
                    <Grid Height="72">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CoverVisualStateGroup">
                                <VisualState x:Name="Narrow"/>
                                <VisualState x:Name="Normal">
                                    <VisualState.StateTriggers>
                                        <AdaptiveTrigger MinWindowWidth="500"/>
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="CoverSmallImage.Visibility" Value="Visible"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="32"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0" MinWidth="0" MinHeight="0" HorizontalAlignment="Center" IsChecked="{x:Bind Done,Mode=TwoWay}" Click="FFFk"/>
                        <Image x:Name="CoverSmallImage" Grid.Column="1" Visibility="Collapsed" Width="96"/>
                        <TextBlock Grid.Column="2" VerticalAlignment="Center" Text="{x:Bind Title}"/>
                        <Line Visibility="{x:Bind Done,Converter={StaticResource BooleanToVisibilityConverter},Mode=OneWay}" Grid.Column="2" StrokeThickness="2" Stretch="Fill" VerticalAlignment="Center" Stroke="Black" X2="1"/>
                    </Grid>
                </UserControl>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</Page>
